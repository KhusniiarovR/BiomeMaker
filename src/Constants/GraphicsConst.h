#pragma once

// virtual screen size
constexpr int virtualScreenSizeX = 640;
constexpr int virtualScreenSizeY = 360;

inline bool is2DModeDone = false; // bool to check if camera mode is done

inline bool shouldExit = false; // to quit game 