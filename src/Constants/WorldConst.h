#ifndef WORLDCONST_H
#define WORLDCONST_H

const int TILE_SIZE = 20;
const int CHUNK_SIZE = 16;

const int RENDER_DISTANCE = 8;
const int unloadMargin = 0;

const char BIOME_SYMBOLS[] = { 'F', 'O', 'W', 'S', 'V' };

// todo find perfect chunk/world/tile size

#endif //WORLDCONST_H
